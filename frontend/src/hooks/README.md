# hooks

Hooks de dados reutilizáveis (useAuth, useGames, etc.).

- Devem controlar `data`, `isLoading`, `error`.
- Devem consumir apenas a camada de `services`.
- Marque com `"use client"` quando necessário.
